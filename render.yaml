services:
  - type: web
    name: db-optimization-intelligence-agent
    runtime: python
    plan: free
    buildCommand: cd frontend-react && npm install && npm run build
    startCommand: python -B run_agent.py

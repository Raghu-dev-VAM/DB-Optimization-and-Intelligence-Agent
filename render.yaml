services:
  - type: web
    name: db-optimization-intelligence-agent
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && cd frontend-react && npm install && npm run build
    startCommand: python -B backend/server.py
